<a href="https://bas.dev" target="_blank" rel="noopener">
	<img src="https://bmcdn.nl/assets/branding/logo.svg" alt="Bas-logo" height="48"/>
</a>

---

# `@basmilius/routing`

Vue Router wrapper that extends `vue-router` with overlay, modal and slide-over
routing. The URL changes to the overlay route, while the background route stays
in view. Browser back closes the overlay.

## ⭐️ Prerequisites

- Bun >= 1.3.0
- Node >= 25

## 🚀 Getting started

1. Run `bun install` in the root of the project.
2. Run `bun --cwd packages/routing build` to build the project.
