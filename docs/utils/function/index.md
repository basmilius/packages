---
outline: deep
---

# Function

Tiny utilities for asynchronous control flow.

- [`debounce`](/utils/function/debounce) — debounces a function, returning a `Promise` that resolves with the eventual result.
- [`waitFor`](/utils/function/waitFor) — promisified `setTimeout`.
